{
  "name": "NullPtr",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "start": "if [ -n \"$DISCORD_BOT_TOKEN\" ] && [ -n \"$TELEGRAM_BOT_TOKEN\" ]; then turbo run dev; elif [ -n \"$DISCORD_BOT_TOKEN\" ]; then turbo run dev --filter=discord; elif [ -n \"$TELEGRAM_BOT_TOKEN\" ]; then turbo run dev --filter=telegram; else echo 'No token was set!'; fi",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
